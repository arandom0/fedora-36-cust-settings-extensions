These are my custom gnome tweaks, extensions and extension settings. The idea here is you pin applications to the start menu (arc menu), as opposed to pinning them directly to the panel. Panel is vertical because it makes 0 sense to have a horizontal panel on a 16:9 display. It's on the right because it's more efficient that way based on my experience. You can of course change all that. This is just how it comes with all the per-configured settings applied.

The codes below need to be saved as scripts. So copy each text and save it as "install.sh" and "settings.py" respectively. Then open a terminal wherever you saved them and run
